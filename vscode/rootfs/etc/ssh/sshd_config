# This file is rendered at runtime based on add-on options. Defaults favor key-based
# authentication and run on port 2222 to avoid conflicts with the Supervisor.
Port 2222
ListenAddress 0.0.0.0
Protocol 2

# Host keys are persisted under /data/ssh/host_keys and symlinked into /etc/ssh/.
HostKey /etc/ssh/ssh_host_ed25519_key
HostKey /etc/ssh/ssh_host_rsa_key
HostKey /etc/ssh/ssh_host_ecdsa_key

PermitRootLogin prohibit-password
PasswordAuthentication no
PubkeyAuthentication yes
AuthorizedKeysFile /data/ssh/authorized_keys
ChallengeResponseAuthentication no
UsePAM no
PrintMotd no
X11Forwarding no
Subsystem sftp internal-sftp
